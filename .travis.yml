sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t loveisall/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true loveisall/react-test npm test

after_success:
  - docker build -t loveisall/multi-client ./client
  - docker build -t loveisall/multi-nginx ./nginx
  - docker build -t loveisall/multi-server ./server
  - docker build -t loveisall/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push loveisall/multi-client
  - docker push loveisall/multi-nginx
  - docker push loveisall/multi-server
  - docker push loveisall/multi-worker
